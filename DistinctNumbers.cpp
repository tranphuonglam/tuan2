#include <iostream>
#include <cmath>

using namespace std;

int main() {
	int x = 0;
	int a;
	//cin >> x;
	while (x != -1) {
	cin >> x;
	if(x != a) {
		cout << x << " " ;
		//printf("%d ", x);
		a = x;
		}
	} 
	
	return 0;
	}
